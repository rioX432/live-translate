{
  "skill_name": "decompose",
  "evals": [
    {
      "id": 1,
      "prompt": "Decompose: add a favorites feature to a KMP app — a data model, a repository, a use case, a Compose screen, and tests.",
      "expected_output": "Behavior slices with the shared prerequisite first, tests inside each slice, each with a Verify command",
      "expectations": [
        "The shared data model (and its mapper) is a prerequisite subtask ordered before the slices that need it",
        "Each remaining subtask delivers one user-visible behavior through the layers it needs, following the KMP layer order inside the subtask",
        "Implementation and its test are the same task; there is no separate tests-only subtask",
        "Cross-cutting checks (lint, format) are the last task and depend on the rest",
        "Every task states What, Where with a file path, How, Why, and Verify"
      ]
    },
    {
      "id": 2,
      "prompt": "One of the decomposed tasks is \"Implement the sync engine\" and you estimate it at 3 hours.",
      "expected_output": "The task is split by behavior until each part has a Verify step that can be written up front",
      "expectations": [
        "The task is recognized as too large because no single Verify step can prove it",
        "It is split into behavior slices (or a spike first when the approach is uncertain) rather than accepted with a note",
        "Each resulting task keeps a concrete Verify step"
      ]
    },
    {
      "id": 3,
      "prompt": "Show the final task list for a 6-task decomposition and hand off to implementation.",
      "expected_output": "A table with behavior, scope, verify and blocking dependencies, then user confirmation before implementing",
      "expectations": [
        "The output is a table with columns for behavior, scope, Verify and Blocked By",
        "Dependency edges point at earlier task numbers and exist only where one task needs another's output",
        "User confirmation is requested before the implementation loop begins (skipped in autonomous /dev)",
        "Tasks with disjoint file scopes and no dependency are marked parallel rather than everything being serialized"
      ]
    }
  ]
}
