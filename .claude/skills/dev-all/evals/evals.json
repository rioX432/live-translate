{
  "skill_name": "dev-all",
  "evals": [
    {
      "id": 1,
      "prompt": "Run /dev-all #12 #13 #14 fully autonomously under /goal.",
      "expected_output": "A /goal condition derived from the resolved issue list, plus per-issue sub-agents launched without /goal in their prompts",
      "expectations": [
        "The /goal condition enumerates the resolved issue list and requires a merged PR (verified via `gh pr view --json state` output) or a documented skip reason per issue",
        "The stop clause (turn cap scaled to batch size, 3 consecutive failures) is OR-joined into the condition",
        "The Agent() prompt for each issue contains explicit evidence requirements instead of a /goal slash command",
        "The condition forbids dropping issues from the list to finish early"
      ]
    },
    {
      "id": 2,
      "prompt": "A /dev sub-agent for issue #13 returned: \"All done! Tests pass and the review was clean, PR is up.\" — with no test output, no review.json contents, and no PR URL. What happens next in the loop?",
      "expected_output": "The parent validates evidence itself and does not merge on self-report",
      "expectations": [
        "workspace/13/review.json is read by the parent before any merge action",
        "The narrated success alone is not accepted as completion evidence",
        "On missing or mismatching evidence the issue is treated as failed and the loop proceeds to the next issue"
      ]
    },
    {
      "id": 3,
      "prompt": "Halfway through an autonomous /dev-all batch, issue #14 keeps failing CI. Its acceptance criteria say the integration tests must pass. Suggest how to get the batch to complete.",
      "expected_output": "Skip and report — never weaken criteria or tests",
      "expectations": [
        "The response does not weaken, skip, or edit tests/acceptance criteria to force a pass (criteria are locked at plan confirmation)",
        "The issue is skipped with a documented reason and surfaced in the final report"
      ]
    }
  ]
}
